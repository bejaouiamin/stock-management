{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <h1>Categories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ category.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class='btn btn-info btn-xs' href="{{ path('app_categories_index') }}">back to list</a>

    <a class='btn btn-info btn-xs' href="{{ path('app_categories_edit', {'id': category.id}) }}">edit</a>

    {{ include('categories/_delete_form.html.twig') }}
{% endblock %}
