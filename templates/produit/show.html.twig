{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}

<div class="row" style="margin-top: 100px;!important">   
<section class="vh-100" style="background-color:#FFFFFF;">        
    <div class="container h-100" > 

        <div class="text-left"> 
            <h1>Produit</h1>
        </div>

    <table class="table custab table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ produit.slug }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ produit.stock }}</td>
            </tr>


        </tbody>
    </table>

    <a class='btn btn-info btn-xs' href="{{ path('app_produit_index') }}">back to list</a>

    <a class='btn btn-info btn-xs' href="{{ path('app_produit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('produit/_delete_form.html.twig') }}
    </div>
</section>
</div>    
{% endblock %}
